<Window x:Class="EasyRDP.Server.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:EasyRDP.Server.Wpf"
        xmlns:vm="clr-namespace:EasyRDP.Server.Wpf.ViewModels"
        xmlns:cv="clr-namespace:EasyRDP.Server.Wpf.Converters"
        Title="EasyRDP Server (WPF)"
        Width="900" Height="600"
        WindowStartupLocation="CenterScreen"
        StateChanged="OnWindowStateChanged"
        Closing="OnWindowClosing">

    <Window.Resources>
        <cv:BoolToColorConverter x:Key="BoolToColor" />
        <cv:LogLevelToColorConverter x:Key="LogColor" />
        <local:AlyStatusToVisibilityConverter x:Key="AlyStatusToVis" />
        <vm:MainViewModel x:Key="VM" />
    </Window.Resources>

    <DockPanel DataContext="{StaticResource VM}">
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="文件">
                <MenuItem Header="清空日志" Command="{Binding ClearLogCommand}" />
                <Separator />
                <MenuItem Header="退出" Command="{Binding ExitCommand}" />
            </MenuItem>
            <MenuItem Header="帮助">
                <MenuItem Header="关于" Command="{Binding AboutCommand}" />
                <MenuItem Header="{Binding AlyClientUpdateStr, FallbackValue='检查更新'}"
                          Command="{Binding CheckUpdateCommand}"
                          Visibility="{Binding AlyClientStatus, Converter={StaticResource AlyStatusToVis}}" />
            </MenuItem>
        </Menu>

        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <Ellipse Width="10" Height="10" Fill="{Binding IsRunning, Converter={StaticResource BoolToColor}}" Margin="0,0,5,0" />
            </StatusBarItem>
            <StatusBarItem Content="{Binding StatusText}" />
            <Separator />
            <StatusBarItem Content="{Binding Clients.Count, StringFormat='客户端: {0}'}" />
        </StatusBar>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Header="服务配置" Margin="5">
                <StackPanel>
                    <Label>端口</Label>
                    <TextBox Text="{Binding Config.Port, UpdateSourceTrigger=PropertyChanged}" />
                    <Label>认证令牌</Label>
                    <TextBox Text="{Binding Config.AuthToken}" />
                    <Label>压缩类型</Label>
                    <ComboBox SelectedValue="{Binding Config.CompressType}" SelectedValuePath="Content">
                        <ComboBoxItem>Zlib</ComboBoxItem>
                        <ComboBoxItem>None</ComboBoxItem>
                    </ComboBox>
                    <Label>帧率 (1-60)</Label>
                    <TextBox Text="{Binding Config.FrameRate, UpdateSourceTrigger=PropertyChanged}" />
                    <Label>最大客户端 (0=无限)</Label>
                    <TextBox Text="{Binding Config.MaxClients, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Content="▶ 启动服务" Command="{Binding StartCommand}" IsEnabled="{Binding IsStopped}" Margin="0,10,0,5" Height="32" FontWeight="Bold" />
                    <Button Content="■ 停止服务" Command="{Binding StopCommand}" IsEnabled="{Binding IsRunning}" Height="32" FontWeight="Bold" />
                </StackPanel>
            </GroupBox>

            <Grid Grid.Column="1" Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="2*" />
                    <RowDefinition Height="3*" />
                </Grid.RowDefinitions>

                <GroupBox Grid.Row="0" Header="已连接客户端">
                    <ListView ItemsSource="{Binding Clients}" SelectedItem="{Binding SelectedClient}">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="ID" DisplayMemberBinding="{Binding SessionId}" Width="40" />
                                <GridViewColumn Header="IP 地址" DisplayMemberBinding="{Binding RemoteEndPoint}" Width="140" />
                                <GridViewColumn Header="状态" DisplayMemberBinding="{Binding DisplayStatus}" Width="60" />
                                <GridViewColumn Header="帧数" DisplayMemberBinding="{Binding FrameCount}" Width="60" />
                                <GridViewColumn Header="连接时间" DisplayMemberBinding="{Binding ConnectedAt, StringFormat='HH:mm:ss'}" Width="80" />
                            </GridView>
                        </ListView.View>
                        <ListView.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="断开客户端" Command="{Binding DisconnectClientCommand}" />
                            </ContextMenu>
                        </ListView.ContextMenu>
                    </ListView>
                </GroupBox>

                <GroupBox Grid.Row="1" Header="日志">
                    <DockPanel>
                        <Button DockPanel.Dock="Top" Content="清空" Command="{Binding ClearLogCommand}" Height="24" />
                        <ListBox ItemsSource="{Binding LogEntries}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding DisplayText}" Foreground="{Binding Level, Converter={StaticResource LogColor}}" FontFamily="Consolas" FontSize="11" />
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </DockPanel>
                </GroupBox>
            </Grid>
        </Grid>
    </DockPanel>
</Window>
